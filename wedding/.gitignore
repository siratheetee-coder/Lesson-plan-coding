node_modules/
.env
.env.local
.vercel

# runtime data — generated by server (local dev only)
data/registrations.json
data/likes.json

# keep folder structure but ignore uploaded photos
uploads/**
!uploads/.gitkeep
!uploads/guest/
!uploads/guest/.gitkeep
